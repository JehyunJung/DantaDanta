# DantaDanta — 작업 기록

---

## 2026-05-10

### 프로젝트 킥오프

**결정 사항**
- 언어: Python
- API: 한국투자증권(KIS) Open API
- 통신: REST API (주문/조회) + WebSocket (실시간 가격)
- 뉴스 분석: 외부 뉴스 수집 + Claude API 감성 분석

**생성된 문서**
- `docs/draft/요구사항_초안.md` — 요구사항 정의
- `docs/plans/실행플랜_v1.md` — Phase 0~5 실행 계획
- `docs/workflow/작업기록.md` — 이 파일

**미결 사항 (초안 참고)**
- 기본 매매 전략 선택
- 종목 유니버스 정의
- 알림 수단 확정 (텔레그램 유력)
- 백테스팅 포함 여부

**다음 단계**
- Phase 1: KIS API 인증 모듈 구현 (액세스 토큰 발급, 자동 갱신)

---

## 2026-05-10 (2)

### Phase 0 완료 — 프로젝트 환경 초기화

**완료된 작업**
- 프로젝트 디렉토리 구조 생성 (`dantadanta/{api,analysis,news,strategy,engine,notify}`)
- `pyproject.toml` 작성 (uv, Python 3.12+, 40개 의존성)
- `.env.example` 작성 (KIS API 키, 예산, 텔레그램 알림)
- `dantadanta/config.py` 작성 (pydantic-settings, 모의/실거래 URL 분기)
- `README.md`, `.gitignore` 생성
- `uv sync` 완료 — 40개 패키지 설치 확인

**결정 사항**
- `requires-python = ">=3.12"` — pandas-ta가 3.12+ 필요
- uv `dependency-groups.dev` 방식 사용 (구형 `tool.uv.dev-dependencies` 대신)
- 모의투자 기본값 (`KIS_IS_MOCK=true`)

**다음 단계**
- `.env` 파일 생성 후 KIS API 키 입력
- Phase 1: KIS API 인증 모듈 구현

---

## 2026-05-10 (3)

### Phase 1 완료 — KIS API 연동 기반 구축

**완료된 작업**
- `dantadanta/api/auth.py` — TokenManager (액세스 토큰 자동 갱신, WebSocket 접속키)
- `dantadanta/api/rest.py` — KisRestClient (공통 헤더 주입, 401 자동 재시도, KisApiError)
- `dantadanta/api/websocket.py` — KisWebSocketClient (subscribe/stream, 자동 재연결 10회)

**결정 사항**
- 토큰 만료 5분 전 선제 갱신 (만료 직전 요청 실패 방지)
- asyncio.Lock으로 동시 토큰 발급 중복 방지
- WebSocket 메시지: JSON 제어 메시지와 "|" 구분 시세 데이터 분리 파싱

**트러블슈팅**
- 403 오류: 실거래용 APP KEY로 모의투자 URL 호출 → `KIS_IS_MOCK=false` 설정으로 해결
- 토큰 중복 발급(1분 제한): 파일 캐시(`.token_cache.json`) + `lru_cache` 싱글턴으로 해결

**다음 단계**
- `.env` 파일 설정 후 실제 토큰 발급 테스트
- Phase 2: 차트 분석 엔진 (일봉/분봉 데이터 조회 + 기술적 지표)

---

## 2026-05-10 (4)

### Phase 2~5 완료 — 전체 시스템 구현

**참고 자료**: https://github.com/koreainvestment/open-trading-api (TR ID 및 엔드포인트 확인)

**Phase 2 — 시세 조회 + 차트 분석**
- `api/market.py`: 현재가/일봉/분봉 조회 (FHKST01010100, FHKST03010100, FHKST03010200)
- `analysis/indicators.py`: EMA5/20/60, RSI14, MACD, 볼린저밴드 + 0~100 스코어링
- `analysis/screener.py`: 유니버스 스캔 → 점수 기반 종목 추천

**Phase 3 — 뉴스 감성 분석**
- `news/collector.py`: 네이버 금융 RSS / 종목 뉴스 수집
- `news/sentiment.py`: Claude Haiku 감성 분석 (score -1~1)

**Phase 4 — 자동 매매 엔진**
- `api/order.py`: 매수/매도/잔고/매수가능금액 (실전 TTTC / 모의 VTTC 자동 분기)
- `engine/budget.py`: 예산 한도·종목 비율 하드 차단
- `strategy/base.py`: BaseStrategy 인터페이스
- `strategy/ma_cross.py`: EMA 골든/데드크로스 전략
- `engine/trader.py`: 손절(-5%)/익절(+10%) + 신호→감성필터→주문 사이클

**Phase 5 — 운영**
- `notify/telegram.py`: 주문/오류/일간요약 알림
- `__main__.py`: 평일 30분 간격 스케줄러, 15:35 일간 요약

---

## 2026-05-10 (5)

### 웹 대시보드 완료 + 프로젝트 리네임

**완료된 작업**

웹 대시보드 (FastAPI + Next.js)
- `web/api/main.py`: FastAPI 앱, lifespan, CORS
- `web/api/routers/`: account, chart, orders, screener 라우터
- `web/api/ws.py`: WebSocket 실시간 시세 브로드캐스터 (3초 폴링)
- `web/api/database.py` + `models.py`: SQLite 주문내역 영속화
- `web/frontend/`: Next.js 15 App Router, Tailwind CSS
  - 대시보드 (잔고 카드 + 최근 주문)
  - 포지션 (수익률 바차트 + 상세 테이블)
  - 차트 (lightweight-charts 캔들 + EMA/BB + RSI)
  - 주문내역 (필터링 테이블)
  - 스크리너 (점수 기반 종목 목록)

리네임
- `sagopalgo` → `dantadanta` 패키지 전체 리네임
- 루트 폴더 `AI-SagoPalgo` → `DantaDanta` (진행 예정)

---

## 남은 작업

### [x] 1. 루트 폴더 리네임
완료. `/Users/jehyunjung/Codes/DantaDanta`로 이동됨.

### [x] 2. 모의투자 End-to-End 검증
완료 (2026-05-10). 실거래 API 키 기준으로 검증.

**결과**
- KIS 토큰 캐시 재사용 정상
- 삼성전자(005930) 현재가 268,500원 조회 성공
- 일봉 차트 5일 조회 성공
- 계좌 잔고 조회 성공 (미입금 계좌 → 0원)
- FastAPI 서버 정상 기동, `/api/account` · `/api/chart/005930` 응답 확인
- 웹 대시보드 대시보드·차트 페이지 렌더링 확인

**수정된 파일**
- `dantadanta/api/rest.py`: EGW00201(초당 한도) rate limit 재시도 로직 추가
- `dantadanta/__main__.py`: "SagoPalgo" → "DantaDanta" 수정
- `web/api/main.py`: "SagoPalgo" → "DantaDanta" 수정
- `web/api/routers/chart.py`: KIS API 오류 시 502 반환 (500 unhandled 제거)
- `web/frontend/src/app/layout.tsx`: 타이틀 "DantaDanta"
- `web/frontend/src/components/Sidebar.tsx`: 사이드바 로고 "DantaDanta"
- `tests/test_connection.py`: API 호출 간 1초 딜레이 추가 (rate limit 방지)

### [ ] 3. 배포 환경 구성
Oracle Cloud Always Free (ARM 24GB) 또는 Hetzner CX22 기준:

**서버 설정**
- Ubuntu 24.04 LTS
- uv 설치 (`curl -Lsf https://astral.sh/uv | sh`)
- `.env` 파일 배포 (secrets 관리)
- systemd 서비스 등록 (`dantadanta.service`)

**systemd 서비스 예시**
```ini
[Unit]
Description=DantaDanta Trading Bot
After=network.target

[Service]
WorkingDirectory=/home/ubuntu/DantaDanta
ExecStart=/home/ubuntu/.local/bin/uv run python -m dantadanta
Restart=always
RestartSec=10
EnvironmentFile=/home/ubuntu/DantaDanta/.env

[Install]
WantedBy=multi-user.target
```

**웹 대시보드 (nginx 리버스 프록시)**
- FastAPI: port 8000 → nginx `/api/` 프록시
- Next.js: `next build` + `next start` (port 3000) → nginx `/` 프록시
- SSL: Let's Encrypt (`certbot`)

### [ ] 4. 추가 전략 구현 (선택)
- RSI 과매도 반등 전략
- 거래량 급등 감지 전략
- 백테스팅 모듈 (`backtesting/`)

### [ ] 5. 모니터링 (선택)
- Grafana + Prometheus (시스템 메트릭)
- 또는 간단히 loguru → 파일 로그 + 텔레그램 일간 요약으로 대체

---

## 주요 명령어 참고

```bash
# 의존성 설치
uv sync

# 트레이딩 봇 실행
uv run python -m dantadanta

# 웹 API 서버
uv run uvicorn web.api.main:app --reload

# 웹 프론트엔드
cd web/frontend && npm run dev

# 연결 테스트
uv run python tests/test_connection.py
```
